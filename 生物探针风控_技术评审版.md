# 生物探针风控体系技术

## 1. 文档信息

- 文档目标: 用于技术评审与落地实施，不包含未经确认的业务事实或效果数据。
- 事实来源: 仅基于图 `风控.png` 中已出现的模块与要素。

## 2. 评审范围与非范围

### 2.1 评审范围

- 双任务判定链路:
  - 任务A: `是不是真人`
  - 任务B: `是不是本人`
- 数据输入:
  - 传感器类、按压类、操作链路类、行为轨迹类、设备维度类、其他非敏感类
- 模型层:
  - 传感器特征模型
  - 传感器时序模型
  - 生物特征模型
  - 行为特征模型
  - 行为习惯模型
- 决策与画像:
  - 多模型打分融合
  - 用户画像沉淀与更新

### 2.2 非评审范围

- 具体阈值、具体拦截策略参数、具体线上效果数值
- 合规结论本身 (仅给出合规检查点，不代替法务结论)
- 任何未在数据字典中确认可采集的字段

## 3. 架构总览

### 3.1 目标分层

- 第一层: `真人性判定`  
  识别脚本、回放、模拟环境、机械化操作。
- 第二层: `本人性判定`  
  识别设备在手但操作者并非账号本人的情形。

### 3.2 逻辑链路

`端上采集 -> 事件网关 -> 特征服务 -> 模型服务 -> 决策引擎 -> 画像服务 -> 审计与回流`

### 3.3 关键原则

- 模型输出概率，策略层做最终处置。
- 任何拦截必须可解释、可回放、可审计。
- 数据、特征、策略均需版本化。

## 4. 数据采集与事件规范

## 4.1 统一事件包络 (Envelope)

所有事件必须先满足统一包络，再附加业务载荷。

| 字段 | 类型 | 必填 | 示例 | 说明 |
|---|---|---|---|---|
| event_id | string | 是 | uuid | 事件唯一ID |
| user_id | string | 是 | hash_xxx | 用户标识，需按合规要求脱敏 |
| session_id | string | 是 | sess_xxx | 会话标识 |
| device_id | string | 是 | dev_xxx | 设备标识，口径固定 |
| app_id | string | 是 | com.xxx.app | 应用标识 |
| app_version | string | 是 | 8.2.1 | 版本分层必需 |
| os_type | string | 是 | android/ios | 操作系统 |
| os_version | string | 是 | 14 | 系统版本 |
| event_type | string | 是 | sensor/touch/route | 事件类型 |
| event_time_ms | int64 | 是 | 1730000000123 | 端侧时间戳 |
| server_recv_time_ms | int64 | 是 | 1730000000456 | 服务端接收时间 |
| trace_id | string | 否 | trace_xxx | 端到端链路追踪 |
| schema_version | string | 是 | v1 | 数据结构版本 |

## 4.2 数据点到事件类型映射

| 图中数据点 | 建议事件类型 | 说明 |
|---|---|---|
| 传感器数据 | sensor_motion | 泛指传感器基础数据 |
| 加速度传感数据 | sensor_accel | 三轴加速度序列 |
| 陀螺仪传感数据 | sensor_gyro | 三轴角速度序列 |
| 磁场传感器数据 | sensor_mag | 三轴磁场序列 |
| 按压力度数据 | touch_pressure | 触控压力、面积、时长 |
| 滑动行为数据 | gesture_swipe | 手势轨迹点序列 |
| 操作链路数据 | action_route | 页面与动作序列 |
| 行为轨迹数据 | behavior_trace | 点击、停留、切换等行为流 |
| 设备维度/纬度数据 | device_profile | 设备环境与指纹维度 |
| 其他非敏感数据 | context_non_sensitive | 非敏感上下文补充信号 |

说明: 图中存在“设备纬度数据”表述，确认是否应为“设备维度数据”。

## 4.3 关键载荷字段定义模板

### 4.3.1 `sensor_accel` / `sensor_gyro` / `sensor_mag`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| sample_rate_hz | float | 是 | 采样率，需落在允许区间 |
| axis_x | float[] | 是 | X轴序列，等长 |
| axis_y | float[] | 是 | Y轴序列，等长 |
| axis_z | float[] | 是 | Z轴序列，等长 |
| start_time_ms | int64 | 是 | 窗口开始时间 |
| end_time_ms | int64 | 是 | 窗口结束时间 |
| window_id | string | 是 | 窗口唯一ID |

### 4.3.2 `touch_pressure`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pointer_id | int | 是 | 触点ID |
| pressure | float | 是 | 压力值 |
| touch_major | float | 否 | 接触面积主轴 |
| touch_minor | float | 否 | 接触面积次轴 |
| down_time_ms | int64 | 是 | 按下时间 |
| up_time_ms | int64 | 是 | 抬起时间 |
| action_seq | int | 是 | 事件序号 |

### 4.3.3 `gesture_swipe`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| gesture_id | string | 是 | 手势ID |
| points | object[] | 是 | 点序列，含x/y/t |
| total_duration_ms | int | 是 | 手势时长 |
| path_length_px | float | 是 | 路径长度 |
| ui_region_id | string | 否 | UI区域标识，便于归一化 |

### 4.3.4 `action_route` / `behavior_trace`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| step_index | int | 是 | 步骤序号 |
| page_id | string | 是 | 页面标识 |
| action_id | string | 是 | 动作标识 |
| action_time_ms | int64 | 是 | 动作发生时间 |
| dwell_time_ms | int | 否 | 停留时长 |
| prev_step_gap_ms | int | 否 | 与上一步时间间隔 |

### 4.3.5 `device_profile`

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| model | string | 是 | 机型 |
| manufacturer | string | 否 | 厂商 |
| abi | string | 否 | CPU架构 |
| emulator_signal | bool | 否 | 模拟器迹象 |
| root_jailbreak_signal | bool | 否 | 提权迹象 |
| debug_signal | bool | 否 | 调试迹象 |
| sensor_count | int | 否 | 传感器数量 |
| tz | string | 否 | 时区 |
| locale | string | 否 | 地区语言 |

## 4.4 数据质量门禁

- 完整性: 必填字段缺失率 `<= [TBD]%`
- 时序性: `event_time_ms` 与 `server_recv_time_ms` 允许偏移 `<= [TBD]ms`
- 合法性: 枚举字段非法值率 `<= [TBD]%`
- 稳定性: 关键字段日波动告警阈值 `> [TBD]`
- 版本一致性: `schema_version` 与特征计算版本严格对齐

## 5. 特征工程设计

## 5.1 特征分组

- 统计特征: 均值、方差、分位数、峰度、偏度、零交叉率
- 频域特征: 主频能量、带宽能量占比、频谱熵
- 轨迹特征: 速度分布、加速度分布、曲率、拐点密度
- 链路特征: 步骤熵、回退率、中断率、关键步骤耗时
- 设备特征: 设备稳定度、环境一致性、风险信号聚合
- 画像偏离特征: 当前会话与历史基线的偏离幅度

## 5.2 窗口策略模板

| 特征域 | 窗口长度 | 步长 | 备注 |
|---|---|---|---|
| 传感器时序 | [TBD] ms | [TBD] ms | 需兼顾实时性与稳定性 |
| 手势行为 | 单手势/多手势聚合 | N/A | 按业务动作边界切分 |
| 链路行为 | 单会话 | N/A | 路径序列完整性优先 |
| 习惯画像 | 近7/30天 | 日级 | 用于长期偏离检测 |

## 5.3 特征字典模板

| feature_name | feature_group | source_event | calc_version | null_policy | drift_monitor |
|---|---|---|---|---|---|
| accel_energy_band_1 | sensor_freq | sensor_accel | v1 | fill_zero | yes |
| pressure_rise_slope_p50 | touch_behavior | touch_pressure | v1 | drop_row | yes |
| route_entropy | route_behavior | action_route | v1 | fill_default | yes |
| swipe_curvature_mean | gesture_behavior | gesture_swipe | v1 | drop_row | yes |
| device_risk_score_raw | device | device_profile | v1 | fill_default | yes |

说明: 表中示例用于字段命名规范展示，不代表当前已上线特征。

## 6. 模型体系设计

## 6.1 模型职责矩阵

| 模型 | 输入 | 输出 | 服务目标 |
|---|---|---|---|
| 传感器特征模型 | 统计/频域特征 | score_live_human | 辅助真人性判定 |
| 传感器时序模型 | 原始或轻加工序列 | score_live_sequence | 识别回放与机械时序 |
| 生物特征模型 | 个体动作相关特征 | score_identity_bio | 本人性概率 |
| 行为特征模型 | 手势/链路/轨迹特征 | score_identity_behavior | 本人性概率 |
| 行为习惯模型 | 跨会话长期特征 | score_habit_deviation | 异常偏离程度 |

## 6.2 训练与验证要求

- 标签定义:
  - `live_label`: 真人/非真人
  - `identity_label`: 本人/非本人
- 标签来源: 人审、挑战验证结果、申诉回流、案件复盘
- 数据切分: 时间切分优先，防止信息泄露
- 分层验证: 按机型、OS版本、渠道、网络环境分层评估
- 校准: 对输出分数进行概率校准，避免阈值不稳定

## 6.3 上线门禁模板

| 指标 | 门禁阈值 | 备注 |
|---|---|---|
| AUC | >= [TBD] | 以离线验证集为准 |
| PR-AUC | >= [TBD] | 不平衡场景必看 |
| 线上延迟P95 | <= [TBD] ms | 含特征与模型推理 |
| 失败回退率 | <= [TBD]% | 模型不可用时兜底策略 |
| 特征覆盖率 | >= [TBD]% | 核心特征可用性 |

## 7. 决策引擎设计

## 7.1 决策输入

- `score_live_human`
- `score_live_sequence`
- `score_identity_bio`
- `score_identity_behavior`
- `score_habit_deviation`
- `device_risk_score`
- `history_trust_score`

## 7.2 决策输出

| outcome | 含义 | 建议动作 |
|---|---|---|
| PASS | 低风险 | 放行 |
| CHALLENGE | 中风险 | 二次校验 |
| REJECT | 高风险 | 拦截 |

## 7.3 策略版本化

- strategy_id
- strategy_version
- publish_time
- rollback_version
- changelog

要求: 任意一次决策可按 `trace_id + strategy_version` 完整回放。

## 8. 用户画像设计

## 8.1 画像结构

| 层级 | 内容 | 更新频率 |
|---|---|---|
| 静态画像 | 设备生态、长期稳定特征 | 低频 |
| 动态画像 | 最近会话行为统计 | 高频 |
| 风险画像 | 历史风险事件与处置轨迹 | 事件触发 |

## 8.2 存储模型建议

- 主键: `user_id`
- 次键: `device_id`, `profile_version`
- 字段分区: `stable_profile`, `recent_profile`, `risk_profile`
- 时间字段: `last_update_time`, `ttl_expire_time`

## 8.3 更新原则

- 低风险样本可增量更新
- 高风险样本需校验后写入
- 画像冲突时采用时间+置信度合并策略

## 9. 服务接口定义 (评审模板)

## 9.1 采集接入接口

`POST /risk/events/ingest`

请求体:

```json
{
  "event_id": "uuid",
  "user_id": "hash_xxx",
  "session_id": "sess_xxx",
  "event_type": "sensor_accel",
  "schema_version": "v1",
  "payload": {}
}
```

响应:

```json
{
  "code": 0,
  "message": "ok"
}
```

## 9.2 实时评分接口

`POST /risk/score/realtime`

请求体:

```json
{
  "trace_id": "trace_xxx",
  "user_id": "hash_xxx",
  "session_id": "sess_xxx",
  "scene_id": "login_or_pay_tbd",
  "feature_version": "v1",
  "strategy_version": "s1"
}
```

响应:

```json
{
  "code": 0,
  "risk_result": {
    "outcome": "PASS",
    "risk_level": "LOW",
    "scores": {
      "score_live_human": 0.0,
      "score_identity_bio": 0.0
    },
    "strategy_id": "strategy_xxx",
    "strategy_version": "s1"
  }
}
```

## 9.3 结果回流接口

`POST /risk/feedback`

请求体:

```json
{
  "trace_id": "trace_xxx",
  "final_label": "fraud_or_legit",
  "label_source": "manual_or_challenge",
  "event_time_ms": 1730000000000
}
```

## 10. 监控看板模板

## 10.1 数据质量看板

| 指标 | 口径 | 告警阈值 |
|---|---|---|
| 事件接收QPS | 分钟级 | [TBD] |
| 关键字段缺失率 | 日级 | > [TBD]% |
| 事件时钟偏移P95 | 日级 | > [TBD]ms |
| schema版本分布异常 | 日级 | 偏离基线> [TBD] |

## 10.2 模型效果看板

| 指标 | 口径 | 维度 |
|---|---|---|
| AUC/PR-AUC | 日/周 | 模型版本 |
| 分数分布漂移PSI | 日级 | 人群分层 |
| 拦截率/误杀率 | 实时+日级 | 场景/渠道 |
| 二次校验通过率 | 日级 | 设备层/用户层 |

## 10.3 策略与业务看板

| 指标 | 口径 | 目标 |
|---|---|---|
| PASS/CHALLENGE/REJECT占比 | 实时 | 结构稳定 |
| 人工审核量 | 日级 | 可承载 |
| 申诉率与申诉通过率 | 周级 | 可控 |
| 风险损失变化 | 周/月 | 下降趋势 |

## 11. 压测与稳定性要求

- 链路延迟: 端到端 `P95 <= [TBD] ms`
- 服务可用性: `>= [TBD]%`
- 峰值容量: `QPS >= [TBD]`
- 降级策略:
  - 特征服务不可用 -> 使用规则兜底
  - 模型服务超时 -> 回退基础策略
  - 画像读取失败 -> 使用会话内特征决策

## 12. 合规与安全检查点

- 数据最小化: 仅采集风控必要字段
- 字段分级: 敏感/准敏感/非敏感分级管理
- 加密要求: 传输与存储均加密，密钥轮转机制
- 权限隔离: 采集、建模、查询、导出分权
- 审计留痕: 策略变更、模型变更、访问变更均留痕

## 13. 风险清单与待确认项

| 编号 | 待确认项 | 影响 |
|---|---|---|
| R1 | “设备纬度”是否应为“设备维度” | 字段口径一致性 |
| R2 | 各端可采集字段清单与系统差异 | 特征可用率 |
| R3 | 标签来源与可信度分级 | 模型有效性 |
| R4 | 误杀与放过成本函数 | 阈值设定 |
| R5 | 二次验证资源与成本 | 策略可执行性 |
| R6 | 延迟预算与部署拓扑 | 线上性能 |
| R7 | 合规红线字段 | 采集边界 |

## 14. 里程碑计划 (模板)

| 阶段 | 产出 | 进入条件 | 退出条件 |
|---|---|---|---|
| M1 数据冻结 | 数据字典v1 | 埋点评审完成 | 缺失率达标 |
| M2 基线建模 | 双任务基线模型 | 标签集可用 | 离线门禁通过 |
| M3 决策联调 | 决策引擎v1 | 模型服务稳定 | 回放可审计 |
| M4 灰度上线 | 小流量灰度 | 全链路监控就绪 | 指标稳定 |
| M5 全量与优化 | 全量策略 | 灰度达标 | 周期性复盘机制建立 |



